# We need to inject this into the environment before nodejs can parse any of our files
# As such, this is also an .env file that gets injected during build.
#
# This is the only way to solve local imports of cloudflare:workers outside of workerd
IS_SSG=true