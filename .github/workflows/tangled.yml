# Mirror everything to Tangled
name: Tangled Bangled

on:
  workflow_call:
  push:
    branches: "main"
  workflow_dispatch: {}

permissions:
  contents: read
  deployments: write

jobs:
  push:
    name: Tangled
    uses: socksthewolf/workflows/.github/workflows/deploy_tangled.yml@main
    secrets: inherit
