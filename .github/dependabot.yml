version: 2
updates:
  # node updates
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "cron"
      # Run biweekly instead of monthly
      cronjob: "0 1 * * 1/2"
    open-pull-requests-limit: 5